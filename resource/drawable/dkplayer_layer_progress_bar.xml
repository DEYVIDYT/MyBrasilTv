<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="?attr/colorSurfaceContainerHighest"/> <!-- Cor de fundo da track -->
        </shape>
    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape>
                <solid android:color="?attr/colorPrimaryContainer"/> <!-- Progresso secundário (buffer) -->
            </shape>
        </clip>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="?attr/colorPrimary"/> <!-- Progresso principal -->
            </shape>
        </clip>
    </item>
</layer-list>
